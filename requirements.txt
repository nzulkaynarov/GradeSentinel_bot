# ВЕРХНЕУРОВНЕВЫЕ зависимости («source»). Фактически в CI и прод ставится
# requirements.lock (точные пины + хеши всех транзитивных зависимостей).
# Регенерация lock после правки этого файла:
#   uv pip compile requirements.txt --universal --python-version 3.12 \
#       --generate-hashes -o requirements.lock
# WebApp ставится из ЭТОГО же файла (отдельного webapp/requirements.txt больше нет).

pyTelegramBotAPI==4.21.0
google-api-python-client==2.149.0
google-auth==2.35.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0
python-dotenv==1.0.1
anthropic>=0.50,<1.0
flask==3.0.3

# PDF экспорт дашборда (pure-python, не требует system libs). Кириллица —
# через DejaVuSans (fonts-dejavu из Ubuntu base) с fallback на Helvetica.
reportlab==4.2.5

# Production WSGI server для webapp (Werkzeug dev server слишком slow и single-thread)
gunicorn==23.0.0

# PostgreSQL: драйвер + пул соединений + миграции схемы.
# Миграция SQLite → PostgreSQL (2026-06-29). [binary] — готовые wheels, без
# system libpq-dev. Пул нужен, т.к. БД теперь по сети (DB-VPS через WireGuard).
psycopg[binary]>=3.2,<4
psycopg-pool>=3.2,<4
alembic>=1.13,<2

# Опциональный observability — активен только если задан SENTRY_DSN в env
sentry-sdk==2.18.0

# Dev / тесты — не ставятся в проде, но нужны локально
pytest==8.3.3
